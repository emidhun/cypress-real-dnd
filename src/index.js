/**
 * Convenience aggregate entry point.
 *
 * Most users should import the plugin and commands separately:
 *   - `require('cypress-real-dnd/plugin')` in cypress.config.js
 *   - `import 'cypress-real-dnd/commands'` in support file
 */
module.exports = require("./plugin");
